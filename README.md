# Build instructions for C++ Poker Engine
If you wish to compile the C++ PokerEngine on a system with CMake and a modern C++ compiler installed (such as GCC or Clang):

```bash
cd poker_engine
mkdir build
cd build
cmake ..
cmake --build .

# Run the test executable
./poker_engine
```
